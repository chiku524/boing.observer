# Boing Network RPC endpoints (HTTP POST, JSON-RPC 2.0).
# Do not commit production URLs; use placeholders or CI secrets.

# Testnet / devnet RPC (e.g. https://testnet-rpc.boing.network/)
NEXT_PUBLIC_TESTNET_RPC=http://localhost:8545

# Mainnet RPC (when available; can point to same as testnet until launch)
NEXT_PUBLIC_MAINNET_RPC=http://localhost:8545

# Optional: Search engine verification (Google Search Console, Bing Webmaster Tools)
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-google-verification-code
# NEXT_PUBLIC_BING_SITE_VERIFICATION=your-bing-verification-code
